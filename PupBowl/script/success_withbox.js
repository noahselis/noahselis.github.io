"use strict";

$(document).ready(() =>
  $("#slider").bxSlider({
    auto: true,
    autoControls: true,
    captions: true,
    minSlides: 1,
    maxSlides: 1,
    slideWidth: 620,
    slideMargin: 10,
  })
);
